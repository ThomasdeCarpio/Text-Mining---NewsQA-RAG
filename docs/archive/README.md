# Archived course material

Các thư mục `explainer/`, `presentation/` và `slides/` là artifact thuyết trình
cũ. Đường dẫn hoặc ảnh chụp trong đó có thể phản ánh phiên bản repo trước đây.
Hãy dùng tài liệu ở `docs/` cấp trên cho workflow hiện tại.

