# Evaluation manifests

Generated selection and variant manifests are committed here. They contain
source revisions, selected article hashes, configuration snapshots, and hashes
of the Git-ignored data artifacts. Do not manually edit generated manifests.

See `docs/evaluation_dataset.md` for the two-stage construction workflow.
