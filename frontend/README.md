# NewsQA RAG frontend

React, TypeScript, and Vite client for chat, retrieval inspection, and the
evaluation dashboard.

```bash
npm install
npm run dev
```

The development server runs at `http://localhost:5173` and expects the FastAPI
backend at `http://localhost:8000`. Configure overrides in `.env` using the
variables documented in `.env.example`.
